packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1 # Specify a version for dbt_utils compatible with dbt 1.9.x
  # - package: dbt-expectations/dbt_expectations
  #   version: [">=0.9.0", "<0.10.0"] # Example if we wanted dbt-expectations
  # For now, only dbt_utils as per the problem description's hint.
